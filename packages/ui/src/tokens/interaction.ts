/** Shared control dimensions in density-independent pixels. */
export const controlSizes = {
  compact: 36,
  default: 44,
  touch: 48,
} as const;

/** Shared motion timings in milliseconds. */
export const motionDurations = {
  fast: 150,
  standard: 220,
  deliberate: 300,
} as const;
